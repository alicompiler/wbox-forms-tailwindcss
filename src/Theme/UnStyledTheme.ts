import {Theme} from "./ThemeContext";

export const unStyled: Theme = {
    inputClassName: 'mt-1 block w-full',
    textAreaClassName: 'mt-1 block w-full',
    selectClassName: 'block w-full mt-1',
    checkboxClassName : 'inline-flex items-center'
}